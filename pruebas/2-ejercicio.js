/*Create an array of three items, and store it in a variable called myArray. The items can be anything you want — how about your favorite foods or bands?
Next, modify the first two items in the array using bracket notation.
Finally, add a new item to the beginning of the array*/

let myArray = ["lasaña", "Helado", "Jugo"];

 myArray [0]= "Spaguettis";
 myArray [1]= "Malteada";
 myArray.unshift ("Dona");

 
 console.log (myArray);

/* crear una funcion  que reciba un numero del 1 al 10 retornar un array con cantidad de items segun el numero que le pase con n items
con valores de 0 a 100 de forma aleatoria con random*/

let n;
let array = n.isArray().ma;
function recibir(n){
    console.log(n)
}

/* 
En una funcion recibimos un array de edades , devolver verdadero o falso dependiendo e si hay un menor de edad
*/

let arrayEdades = [45, 5, 16, 45];
function devolver(arrayEdades){
        for (let i =0; i < arrayEdades.length; i++){
            if (arrayEdades <18){
                return true;
            }else{
                return false;
            }
}
}
console.log (devolver(arrayEdades));

/* crear una funcion que me va a contar la cantidad de mayores de edad que haya*/

let arrayEdades2 =[1, 4, 89 ,9];
function contar(arrayEdades){
    for (let i=0; i <arrayEdades2.length;i++){
        if (arrayEdades2 <18){
         const contador = arrayEdades2 +1;
        }
    }
}
 console.log (contar(arrayEdades2 + contador));

 //funcion que reciba un array con edades, funcion devolver array promedio solo de persona mayor de edad 

 let arrayEdades3 = [89,30,6,9];
 
 function promedio(arrayEdades3){
    if (arrayEdades3 >18 +)

        
    }
 